```javascript
// Solution: Using values from Tailwind's predefined scale
<div class="bg-gradient-to-r from-blue-500 to-purple-500 p-4">
  <p>This now correctly shows the gradient.</p>
</div>

// Alternative solution: If you need a custom color, add it to your theme file
// Then use the color from the custom theme.
```